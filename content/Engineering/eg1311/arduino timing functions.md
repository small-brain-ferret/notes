```C++
void delay(milisecs);
void delayMicroseconds(microsecs);
// delays execution

unsigned long millis(); // 50 days
unsigned long micros(); // 1 hour
// no input args, query the onboard clock and asks how long has elapsed since the Arduino was turned on
```
