```C++
void pintMode(pin, INPUT/OUTPUT);

void digitalWrite(pin, HIGH/LOW);

int digitalRead(pin);

// if pin mode is set to input, you can read what the voltage is on a pin at a particular time. This outputs either LOW or HIGH
```
