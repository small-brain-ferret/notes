Standard form:

$$
\frac{(x-x_0)^2}{a^2}-\frac{(y-y_0)^2}{b^2}=1  
$$

Properties:

- centre: $(x_0,y_0)$
- opens horizontally
- horizontal distance from centre to each vertex: $a$

If instead:

$$
\frac{(y-y_0)^2}{b^2}-\frac{(x-x_0)^2}{a^2}=1  
$$

then the hyperbola opens vertically.
