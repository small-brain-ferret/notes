For functions of 2 variables $f(x,y)$, we can draw a surface $z=f(x,y)$.
This can be difficult to visualise.

Thus, we alternatively use [[Contours]], curves on which the function has a given constant value.

For higher dimensions, [[level sets]] are used.
